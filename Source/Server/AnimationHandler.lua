return {
    --// Fires every time Framework:Connect(player) is called
    Connect = function(framework, player)

    end,
    --// Fires every time Framework:Disconnect(player) is called
    Disconnect = function(framework, player)

    end,
    
}